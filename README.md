# AI Resume & Cover Letter Generator

A Node.js based project to generate AI-powered resumes and cover letters.

## Features
- Resume analysis with ATS scoring
- Cover letter generation
- File upload support

## Setup
1. Clone the repo
2. Run `npm install`
3. Add your API keys in `.env`
4. Run `npm start`
